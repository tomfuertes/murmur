# Cloudflare account ID — wrangler reads this automatically when not set in wrangler.jsonc
CLOUDFLARE_ACCOUNT_ID=

# Turnstile bot protection — set as Workers env vars in Cloudflare dashboard
# TURNSTILE_API_KEY (plaintext) — public site key, sent to clients via WebSocket
# TURNSTILE_API_SECRET (secret) — server-side verification key
