# Cloudflare account ID — wrangler reads this automatically when not set in wrangler.jsonc
CLOUDFLARE_ACCOUNT_ID=

# Turnstile bot protection (client-side key, exposed via Vite)
VITE_TURNSTILE_API_KEY=

# Turnstile secret (server-side) — set in .dev.vars for local, wrangler secret for prod
# TURNSTILE_API_SECRET goes in .dev.vars (not here — secrets must not be in client-visible env)
